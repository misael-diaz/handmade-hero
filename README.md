# handmade-hero
following Casey's game dev tutorial

## Requirements

To compile the game you must install MinGW.

## Compilation

From the MS DOS prompt execute the following command line:

```sh
gcc -o game.exe game.c -Wl,--subsystem,windows -lgdi32
```
